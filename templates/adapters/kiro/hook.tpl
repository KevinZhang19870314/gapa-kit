{
  "version": "v1",
  "hooks": [
    {
      "name": "{{slot:hookName}}",
      "trigger": "{{slot:triggerType}}",
      "action": {
        "type": "agent",
        "prompt": "{{slot:prompt}}"
      }
    }
  ]
}
